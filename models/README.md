# Place your GLB and GLTF 2.0 model files here

This directory is mounted as a volume in the Docker container.
Any `.glb` or `.gltf` files placed here will be available to view in the application.

## Example:
- model1.glb
- robot.glb
- scene.gltf
