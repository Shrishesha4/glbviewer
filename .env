# .env.example
# Copy this file to .env.local for local development

# Admin password for web UI access to /models/explore and /media/explore
# Generate a strong password: openssl rand -base64 32
ADMIN_PASSWORD=Shrishesha@4

# API Key for upload authentication (required for production CDN usage)
# Also allows bypassing admin login for programmatic access
# Generate a strong random key: openssl rand -base64 32
# Leave empty to allow uploads without authentication (not recommended for production)
UPLOAD_API_KEY=Shrishesha@4

# Base URL for the application (used in CDN responses)
# Examples:
#   http://localhost:3000
#   https://models.yourdomain.com
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Node environment
NODE_ENV=production

# Example for custom port (requires modifying next.config.js)
# PORT=3000
